describe('add 関数のテスト', () => {
  it('1 + 1 は 2', () => {
    expect(add(1, 1)).toBe(2)
  })

  it('1 + 4 は 5', () => {
    expect(add(1, 4)).toBe(5)
  })

  it('10 + 2 は 12', () => {
    expect(add(10, 2)).toBe(5)
  })
})